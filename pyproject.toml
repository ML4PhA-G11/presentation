[project]
name = "ml4pha-g11-presentation"
version = "0.1.0"
description = "Figure-prep helpers for the Slidev deck on learning the LBM collision operator."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pillow>=10.0",
]
